name: Weekly MLB Model Health

on:
  schedule:
    # Sundays at 14:00 UTC (~10:00 ET in DST)
    - cron: "0 14 * * 0"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  health-report:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          submodules: false

      - name: Verify workspace layout
        run: |
          pwd
          ls -la
          test -d ml-backend
          test -f ml-backend/mlb_model_health_report.py
          test -f ml-backend/fetch_mlb_stats.py
          test -f ml-backend/train_mlb_ensemble_models.py -o -f ml-backend/train_mlb_models.py

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: ml-backend/requirements.txt

      - name: Install dependencies
        working-directory: ml-backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Determine ET date
        id: etdate
        run: |
          echo "date_et=$(TZ=America/New_York date +%Y%m%d)" >> "$GITHUB_OUTPUT"

      - name: Validate MLB data contract
        working-directory: ml-backend
        run: |
          python validate_mlb_data_contract.py --date "${{ steps.etdate.outputs.date_et }}"

      - name: Resolve MLB trainer script
        id: trainer
        working-directory: ml-backend
        run: |
          if [ -f "train_mlb_ensemble_models.py" ]; then
            echo "trainer_script=train_mlb_ensemble_models.py" >> "$GITHUB_OUTPUT"
          elif [ -f "train_mlb_models.py" ]; then
            echo "trainer_script=train_mlb_models.py" >> "$GITHUB_OUTPUT"
          else
            echo "No MLB trainer script found in ml-backend" >&2
            ls -la
            exit 1
          fi

      - name: Refresh MLB artifacts
        working-directory: ml-backend
        run: |
          python fetch_mlb_stats.py --date "${{ steps.etdate.outputs.date_et }}"
          python "${{ steps.trainer.outputs.trainer_script }}"

      - name: Generate model health report
        working-directory: ml-backend
        run: |
          python mlb_model_health_report.py

      - name: Add report to workflow summary
        if: always()
        run: |
          if [ -f "ml-backend/reports/mlb_model_health_report.md" ]; then
            cat "ml-backend/reports/mlb_model_health_report.md" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "MLB health report markdown was not generated." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload health report artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mlb-model-health-report
          path: |
            ml-backend/reports/mlb_model_health_report.md
            ml-backend/reports/mlb_model_health_report.json
            ml-backend/mlb_ensemble_metrics.json
            ml-backend/mlb_prop_predictions_ml.json
          if-no-files-found: warn
