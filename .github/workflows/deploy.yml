name: Deploy to Live Website

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Copy deployment files to root
      run: |
        cp -r 2-Developer-Tools/godaddy-upload/* .
        ls -la
        
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: .
        cname: betyard.net
        
    - name: Notify deployment
      run: |
        echo "🚀 Advanced Analytics Dashboard deployed to betyard.net"
        echo "✅ Real-time odds comparison now live"
        echo "✅ Arbitrage detection active"
        echo "✅ Market analysis available"