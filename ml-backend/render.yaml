# Render.com Configuration
# This file tells Render how to build and run your ML backend

# Build Command (automatically detected from requirements.txt)
# pip install -r requirements.txt

# Start Command  
# python app.py

# Environment Variables (set in Render dashboard if needed)
# PORT - automatically set by Render
# PYTHON_VERSION - specified in runtime.txt

# Health Check Endpoint
# GET /health - returns {"status": "healthy", "model_loaded": true}

# Auto-deploy: Enabled (deploys on every GitHub push)
# HTTPS: Automatically provided
# Custom Domain: Available (free)
# Sleep Mode: After 15 minutes of inactivity
# Wake Time: ~10-15 seconds on first request

# Free Tier Limits:
# - 750 hours/month runtime  
# - Unlimited bandwidth
# - Unlimited deployments
# - No credit card required

# Production Ready Features:
# - Automatic SSL certificates
# - Global CDN
# - DDoS protection  
# - 99.9% uptime SLA
# - Real-time logs and metrics